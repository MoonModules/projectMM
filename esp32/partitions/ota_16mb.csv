# Shared 16 MB OTA partition table — used by every 16 MB-flash firmware
# regardless of chip (classic ESP32, S3-N16R8, P4-NANO). The data rows below are
# chip-independent: a partition table is a flash-offset map, and 16 MB is 16 MB
# on any of these SoCs, so one layout serves all three (was three byte-identical
# files: esp32_16mb / esp32s3_n16r8 / esp32p4_16mb; see docs/history/decisions.md).
# PSRAM size and flash *mode* differ per board, but those live in the sdkconfig
# fragments, not here.
#
# Layout (16 MB = 0x1000000):
#   0x10000-0x40FFFF   app0   (4 MB)    -> ota_0   (full binary + headroom)
#   0x410000-0x80FFFF  app1   (4 MB)    -> ota_1
#   0x810000-0xFFFFFF  spiffs (~7.9 MB) -> LittleFS state
#
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x400000,
app1,     app,  ota_1,   0x410000, 0x400000,
spiffs,   data, spiffs,  0x810000, 0x7F0000,
