class Button {
  int pin = 0;
  int last = 1;

  void defineControls() {
    addControl("pin", pin, 0, 48);
  }

  void tick20ms() {
    int now = gpioRead(pin);
    if (now != last) {
      last = now;
      setControl("switch1", 1 - now);
    }
  }
}
